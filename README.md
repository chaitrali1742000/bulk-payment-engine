# bulk-payment-engine
A banking-grade payment processing engine built with Java 21, Spring Boot 3.x, and Apache Kafka and demonstrates use of Kafka not just as a simple message bus, but as a distributed log and state store. It handles the ingestion, validation, clearing, and settlement of bulk corporate payments, ensuring zero data loss and strict schema enforcement.
